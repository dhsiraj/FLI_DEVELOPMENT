<h1>Listing users</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Hashed password</th>
    <th>Salt</th>
    <th>Email</th>
    <th>Ip address</th>
    <th>User type</th>
    <th>Last login</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% if getusertype == 'Admin' %>
<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.hashed_password %></td>
    <td><%= user.salt %></td>
    <td><%= user.email %></td>
    <td><%= user.ip_address %></td>
    <td><%= user.user_type %></td>
    <td><%= user.last_login %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
<% end %>
</table>

<br />
<td><%= link_to 'Register', :method => :get , :action => 'create' %> | <%= link_to 'Login', :method => :get, :action => 'signin' %> | 
<%= link_to 'New User', new_user_path %>
